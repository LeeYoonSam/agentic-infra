// API Gateway configuration for <%= name %>
// Generated by Agentic Infra

export interface ApiRoute {
  path: string;
  method: 'GET' | 'POST' | 'PUT' | 'DELETE';
  handler: string;
  auth: boolean;
}

export const routes: ApiRoute[] = [
  { path: '/items', method: 'GET', handler: 'handleGet', auth: false },
  { path: '/items', method: 'POST', handler: 'handlePost', auth: <%= features.includes('auth') ? 'true' : 'false' %> },
  { path: '/items/{id}', method: 'GET', handler: 'handleGet', auth: false },
  { path: '/items/{id}', method: 'PUT', handler: 'handlePut', auth: <%= features.includes('auth') ? 'true' : 'false' %> },
  { path: '/items/{id}', method: 'DELETE', handler: 'handleDelete', auth: <%= features.includes('auth') ? 'true' : 'false' %> },
];

export const corsConfig = {
  allowOrigins: ['*'],
  allowMethods: ['GET', 'POST', 'PUT', 'DELETE', 'OPTIONS'],
  allowHeaders: ['Content-Type', 'Authorization', 'X-Api-Key'],
  maxAge: 86400,
};
